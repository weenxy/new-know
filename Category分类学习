说到分类，先说一下它和extension的区别。
category是在运行期决议的，extension是在编译期决议的。
category不能添加实例变量，extension可以（是私有的）。
category可以给没有源代码的类添加扩展，extension只能给我们自己的类添加。

分类里可以添加属性，但是不会自动生成对应的set/get方法，一般通过绑定对象的方式来实现添加属性。

分类里添加的属性、方法是在什么时候添加到对象上的？
